---
import type { Experiences } from "@/data/experiences";

type Props = Experiences;

const { company, technologies, description, puesto } = Astro.props;

const formatTechs = (values: string[]) =>
  values.toString().replaceAll(",", " • ");
---

<>
  {
      <div class="t group  border border-neutral-700 ">
        <section class="flex w-full flex-col justify-between gap-2 rounded-md p-4 md:flex-row md:items-center">
            <div class="flex flex-col gap-2 md:flex-row md:items-center md:gap-4">
            <p class="text-neutral-100">{company}</p>
            <p class="w-fit rounded-md bg-neutral-900 px-4 py-1">{formatTechs(technologies)}</p>
            </div>
            <p class="w-fit rounded-md bg-blue-300 text-gray-900 px-4 py-1">{puesto}</p>
       
        </section>
        <section>
                <p class="w-full  bg-neutral-700 px-4 py-1">{description}</p>
        </section>
      </div>
      
  }
</>
